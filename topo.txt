APP
├─ README.md                          # Hướng dẫn/ghi chú dự án
├─ topo.txt                           # Sơ đồ dự án
├─ data/                              # Bộ dữ liệu đầu vào (checklist)
│  ├─ canonical_ubuntu_24.04_lts.csv  # Checklist dạng CSV
│  ├─ canonical_ubuntu_24.04_lts.json # Checklist dạng JSON
│  └─ U_CAN_Ubuntu_24-04...xccdf.xml  # Checklist STIG/XCCDF dạng XML
├─ logs/                              # Thư mục chứa log theo từng phiên chạy (mặc định)
├─ pyproject.toml                     # Cấu hình packaging/build & khai báo entry-point CLI
├─ security_app/                      # Package chính
│  ├─ __init__.py
│  ├─ app/
│  │  └─ cli.py                       # Điểm vào CLI: nhận args → parse_file → run_all_rules → print_report
│  ├─ config.py                       # Hằng số: marker, timeout, retries, denylist, top fail, logs dir
│  ├─ models.py                       # Dataclass Rule, CmdResult
│  ├─ core/
│  │  ├─ command.py                   # Chạy 1 lệnh shell → CmdResult (retry/backoff/timeout/denylist)
│  │  ├─ command_extractor.py         # Tách lệnh từ “check” theo marker, hỗ trợ nối dòng "\\"
│  │  ├─ logger.py                    # Ghi log per-rule (KHÔNG ghi summary JSON/CSV nếu theo patch)
│  │  └─ runner/
│  │     ├─ __init__.py               # Orchestration: run_all_rules()
│  │     ├─ extract.py                # _pre_extract_rules(): lọc denylist, gom allowed/denied
│  │     ├─ plan.py                   # _prepare_tasks(): chia task per-command/per-chunk
│  │     ├─ workers.py                # _worker(): thực thi chunk lệnh
│  │     └─ merge.py                  # _merge_and_log(): gộp kết quả, gọi logger
│  ├─ parsers/
│  │  ├─ __init__.py
│  │  ├─ common.py                    # detect_file_type theo phần mở rộng
│  │  ├─ schema.py                    # COL_MAP/REQ_FIELDS dùng chung
│  │  ├─ dispatch.py                  # parse_file(): auto-chọn parser theo loại tệp
│  │  ├─ csv_parser.py                # CSV→Rule (chuẩn hoá cột + title fallback name)
│  │  ├─ json_parser.py               # JSON→Rule (stig.findings)
│  │  └─ xml_parser.py                # XCCDF→Rule (namespace)
│  ├─ policy/
│  │  ├─ safety.py                    # deny_reason(): central denylist
│  │  └─ secrets.py                   # mask_secrets(): compile regex một lần
│  ├─ reporting/
│  │  ├─ stats.py                     # compute_stats(): tổng hợp số liệu
│  │  └─ terminal.py                  # print_report(): render terminal (bảng, bar)
│  └─ utils/
│     └─ text.py                      # _term_width, _safe_name, _bar, _table
├─ security_app.egg-info/             # Metadata do setuptools sinh ra (nếu đã build/install)
└─ reportAPP			      # chứa các file app reactjs
